name: Trigger Jenkins Jo

on:
  push:
    branches:
      - main

jobs:
  trigger_jenkins:
    name: Trigger Jenkins Job
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://172.16.253.11:8080/job/MY_JENKINS_TOKEN/build?token=${{ secrets.MY_JENKINS_TOKEN }}"
            --user "user:123456"