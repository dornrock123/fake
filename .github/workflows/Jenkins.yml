name: Trigger Jenkins Job

on:
  push:
    branches:
      - main

jobs:
  trigger_jenkins:
    name: Trigger Jenkins Job
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://172.16.253.11:8080/job/ci-action-jenkin/build?token=${{ secrets.MY_JENKINS_TOKEN }}" \
            --user "user:11fc7e7d648c13cb35a5de6bcb43393cd5"
